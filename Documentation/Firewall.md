## Firewall Configuration (UFW)

I configured the Uncomplicated Firewall (UFW) to control incoming traffic.

### Rules Applied

* Allowed SSH on custom port
* Denied all other incoming connections by default

### Purpose

This ensures that only necessary services are accessible, reducing the attack surface.
