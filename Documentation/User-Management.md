## User and Group Management

I created user groups to simulate departments within the organization:

* HR
* Finance
* IT

Users were then assigned to their respective groups to enforce role-based access control.

### Commands Used

* addgroup → create groups
* adduser → create users
* usermod → assign users to groups

### Purpose

This ensures that access to resources is controlled based on job roles, which is a key principle in enterprise environments.
